{
  "name": "mix-of-game-soccer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "test": "node --import tsx --test src/engine.test.ts"
  },
  "dependencies": { "phaser": "3.90.0" },
  "devDependencies": { "@types/node": "^22.0.0", "typescript": "~5.9.0", "vite": "^7.0.0", "tsx": "^4.20.0" }
}
